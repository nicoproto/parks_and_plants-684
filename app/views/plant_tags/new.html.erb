<div class="container col-8 mt-5">
  <%= simple_form_for [@plant, @plant_tag] do |f| %>
    <%= f.association :tag, input_html: { class: "select-tags" } %>
    <%= f.submit class: "btn btn-primary" %>
  <% end %>
</div>

